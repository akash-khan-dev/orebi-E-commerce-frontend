import React from "react";

const Heading = ({ title }) => {
  return (
    <h2 className="font-bold text-[40px] text-[#262626] font-primaryFont">
      {title}
    </h2>
  );
};

export default Heading;
